
/* eslint-disable @typescript-eslint/unbound-method */
import { PassportVerifier } from "../src/verifier";

test("Can verify Passport", async () => {
    expect(1).toEqual(1);
});

test("Can verify Stamp", async () => {
    expect(1).toEqual(1);
});

test("Can verify verifiableCredential", async () => {
    expect(1).toEqual(1);
});
