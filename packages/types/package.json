{
  "name": "@gitcoinco/passport-sdk-types",
  "version": "0.1.2",
  "license": "AGPL-3.0",
  "homepage": "https://github.com/gitcoinco/passport-sdk/tree/main/packages/types",
  "types": "src/index.d.ts",
  "scripts": {
    "prepublish": "yarn run build",
    "build": "tsc -b .",
    "clean": "rimraf dist *.tsbuildinfo",
    "lint": "tsc --noEmit && eslint --ext .ts,.js,.tsx .",
    "prettier": "prettier --write .",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "ethers": "^5.0.32",
    "tslint": "^6.1.3",
    "typescript": "~4.6.3"
  }
}
